{
  "entities": {
    "Customer": {
      "title": "Customer",
      "description": "Information about Party A (Buyer) for contract generation.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the customer." },
        "name": { "type": "string", "description": "Official company name." },
        "taxId": { "type": "string", "description": "Unified Social Credit Code / Tax ID." },
        "address": { "type": "string", "description": "Registered address." },
        "phone": { "type": "string", "description": "Contact phone number." },
        "bank": { "type": "string", "description": "Bank name." },
        "account": { "type": "string", "description": "Bank account number." },
        "lastUsed": { "type": "string", "format": "date-time", "description": "The last time this customer was used." },
        "uid": { "type": "string", "description": "The UID of the user who created this customer record." }
      },
      "required": ["name", "taxId", "uid"]
    },
    "User": {
      "title": "User",
      "description": "User profile information and access status.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Unique identifier (Firebase Auth UID)." },
        "email": { "type": "string", "description": "User's email address." },
        "displayName": { "type": "string", "description": "User's display name." },
        "isAdmin": { "type": "boolean", "description": "Whether the user has administrative privileges." },
        "approved": { "type": "boolean", "description": "Whether the user has been approved by an administrator." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Creation timestamp." },
        "lastLoginAt": { "type": "string", "format": "date-time", "description": "Last login timestamp." }
      },
      "required": ["uid", "email", "approved"]
    }
  },
  "firestore": {
    "customers": {
      "schema": "Customer",
      "description": "A collection of customers for auto-filling contract details."
    },
    "users": {
        "schema": "User",
        "description": "A collection of user profiles and approval status."
    }
  }
}
